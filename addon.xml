<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<addon id="script.audiooffsetmanager.evolved" version="1.0.0~beta15" name="Audio Offset Manager: Evolved" provider-name="matthane">
    <requires>
        <import addon="xbmc.python" version="3.0.1"/>
    </requires>
    <extension point="xbmc.service" library="service.py" />
    <extension point="xbmc.python.script" library="script.py" />
    <extension point="xbmc.addon.metadata">
        <source>https://github.com/matthane/script.audiooffsetmanager.evolved</source>
        <news>v1.0.0~beta15 (2026-07-19)
- Seek back on unpause waits half a second and stands down if another addon or a manual seek moves playback first, so the video never jumps back twice after unpausing

v1.0.0~beta14 (2026-07-18)
- HDR types that different Kodi versions report with different spellings now share one saved entry, so an offset learned on one device applies on another and backups restore correctly
- Entries and pending resets saved under an older spelling convert automatically the next time they load
        </news>
        <!-- news is schema-capped at 1500 characters (addon-checker
             enforces it): keep only the last couple of versions here;
             earlier changelogs live in the git history. -->
        <summary lang="en_GB">Kodi's missing lipsync tool</summary>
        <description lang="en_GB">Adjust Kodi's audio offset during playback the way you always have. Audio Offset Manager: Evolved remembers the value for that stream's HDR type, audio format, and frame rate, then re-applies it automatically on every matching playback. Includes seek-back after adjustments, playback notifications, and a management view of everything it has learned.</description>
        <platform>all</platform>
        <license>GPL-3.0-or-later</license>
        <assets>
            <icon>resources/icon.png</icon>
            <fanart>resources/fanart.jpg</fanart>
            <screenshot>resources/aome-screenshot-1.png</screenshot>
            <screenshot>resources/aome-screenshot-2.png</screenshot>
            <screenshot>resources/aome-screenshot-3.png</screenshot>
            <screenshot>resources/aome-screenshot-4.png</screenshot>
            <screenshot>resources/aome-screenshot-5.png</screenshot>
            <screenshot>resources/aome-screenshot-6.jpg</screenshot>
        </assets>
    </extension>
</addon>
