<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<addon id="script.audiooffsetmanagerevolved" version="1.0.0~beta9" name="AOM Evolved" provider-name="matthane">
    <requires>
        <import addon="xbmc.python" version="3.0.1"/>
    </requires>
    <extension point="xbmc.service" library="service.py" />
    <extension point="xbmc.python.script" library="script.py" />
    <extension point="xbmc.addon.metadata">
        <source>https://github.com/matthane/script.audiooffsetmanager</source>
        <news>v1.0.0~beta9 (2026-07-17)
- Manage view groups by HDR type when more than 8 offsets are stored: pick a group to browse its entries, Back returns to the group list; rows inside a group lead with the codec
- Small collections keep the familiar flat list; Clear all stays on the top level
- Notifications no longer vanish when raised during the previous toast's fade-out

v1.0.0~beta8 (2026-07-16)
- Settings changes now take effect during playback: flipping per-refresh-rate offsets or un-pausing re-applies the right offset immediately
- Deleting the playing stream's offset in the manage view now resets the delay right away (idle-time deletes still apply at the next playback)
- A settings save or delete never discards a manual adjustment you are dialing in
        </news>
        <!-- news is schema-capped at 1500 characters (addon-checker
             enforces it): keep only the last couple of versions here;
             earlier changelogs live in the git history. -->
        <summary lang="en_GB">Fix lipsync once — Evolved remembers it per stream profile</summary>
        <description lang="en_GB">Adjust Kodi's audio offset during playback the way you always have. AOM Evolved remembers the value for that stream's HDR type, audio format, and refresh rate, then re-applies it automatically on every matching playback. Includes seek-back after adjustments, playback notifications, and a management view of everything it has learned.</description>
        <platform>all</platform>
        <license>GPL-3.0-or-later</license>
        <assets>
            <icon>resources/icon.png</icon>
            <fanart>resources/fanart.jpg</fanart>
            <screenshot>resources/screenshot-1.jpg</screenshot>
            <screenshot>resources/screenshot-2.jpg</screenshot>
            <screenshot>resources/screenshot-3.jpg</screenshot>
        </assets>
    </extension>
</addon>
