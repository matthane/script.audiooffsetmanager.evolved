"""Dev-only test suite for script.audiooffsetmanager.evolved (export-ignore'd).

Never shipped in the addon package. Split into:
  - unit/     characterization tests pinning current runtime behavior
  - contract/ invariants (settings matrix, string ids) the redesign must keep
"""
